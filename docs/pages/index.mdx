# jack

**Ship before you forget why you started.**

jack is a CLI for vibecoders who want to deploy to the cloud without thinking about infrastructure. One command to create, one command to ship.

{/* Hero image placeholder - replace with gif */}
<div style={{ margin: '2rem 0', padding: '3rem', background: 'var(--vocs-color_background2)', borderRadius: '12px', textAlign: 'center', border: '2px dashed var(--vocs-color_border)' }}>
  <p style={{ margin: 0, color: 'var(--vocs-color_text3)' }}>
    Demo gif coming soon
  </p>
</div>

## Quick Start

```bash
# Install
bun add -g @getjack/jack

# Create a new project
jack new my-app

# Deploy it
jack ship
```

## Why jack?

- **Zero config** - No YAML files, no dashboards, no decisions
- **AI-native** - Built-in MCP server for Claude Code and other agents
- **Fast** - Deploys to edge in seconds
- **Simple** - Three commands: `new`, `ship`, `open`

## Commands

| Command | What it does |
|---------|--------------|
| `jack new` | Create a new project from a template |
| `jack ship` | Deploy your project |
| `jack ls` | List your projects |
| `jack open` | Open your project in the browser |

Ready to start? [Get started →](/getting-started)

---

## For AI Agents

jack docs are optimized for LLMs. Use these URLs for context:

- `llms.txt` - Summary for quick context
- `llms-full.txt` - Full documentation

These files are auto-generated at the root of the docs site (e.g., `https://hellno.github.io/jack/llms.txt`).
