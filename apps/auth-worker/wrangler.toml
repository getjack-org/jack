name = "jack-auth"
main = "src/index.ts"
compatibility_date = "2024-12-01"

routes = [
  { pattern = "auth.getjack.org", custom_domain = true }
]

[vars]
WORKOS_CLIENT_ID = "client_01KD3D0QAQF4YXP4ZS8DX00CW6"

# Secrets (set via `wrangler secret put`):
# - WORKOS_API_KEY

[[unsafe.bindings]]
name = "MAGIC_AUTH_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 5, period = 60 }
